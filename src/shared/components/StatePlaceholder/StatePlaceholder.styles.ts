import type { SxProps, Theme } from '@mui/material/styles';

export const styles: Record<string, SxProps<Theme>> = {
  container: { py: 6, alignItems: 'center', justifyContent: 'center', textAlign: 'center', px: 2 },
  title: { fontWeight: 800 },
};
