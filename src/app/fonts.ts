// Self-hosted variable fonts (Nunito for display, Nunito Sans for body/UI).
// Each package's `index.css` bundles latin, latin-ext, cyrillic, cyrillic-ext,
// and vietnamese subsets in a single variable-weight face, so importing it
// covers both the English (`en`) and Ukrainian (`uk`) UI locales without an
// external font request.
import '@fontsource-variable/nunito/index.css';
import '@fontsource-variable/nunito-sans/index.css';
